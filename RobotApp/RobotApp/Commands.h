#pragma once
#include "utils.h"

bool Place(string strIn)
{

   string strCapitalize = CapitalizeString(strIn);
   return false;
}

bool Left(string strIn)
{

   string strCapitalize = CapitalizeString(strIn);
   return false;
}

bool Right(string strIn)
{

   string strCapitalize = CapitalizeString(strIn);
   return false;
}

bool Report(string strIn)
{

   string strCapitalize = CapitalizeString(strIn);
   return false;
}

bool Move(string strIn)
{

   string strCapitalize = CapitalizeString(strIn);
   return false;
}